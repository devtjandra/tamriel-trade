import 'package:flutter/material.dart';

class Styles {
  static const description = TextStyle(color: Colors.grey, fontSize: 12.0);

  static const panelBorder = BorderRadius.only(topLeft: Radius.circular(30.0), topRight: Radius.circular(30.0));
}